# Default admin password used on first run.
# If the database still contains the seed password ('admin'),
# the app will update it to this value during initialization.
VITE_ADMIN_DEFAULT_PASSWORD=change-me
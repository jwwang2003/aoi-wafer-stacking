// Contant default values are saved here

// The path for user perferences must stay constant! (other paths can be reconfigured & saved in the user pref.)
export const PREFERENCES_FILENAME = 'preferences.json'  // User preferences & settings
export const DATA_SOURCES_CONFIG_FILENAME = 'data_sources.json'  // Data sources (wafer信息的文件路径)
